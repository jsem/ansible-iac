[Unit]
Description={{ backend_name }}
After=syslog.target

[Service]
User=root
ExecStart={{ backend_root }}/{{ backend_name }}/{{ backend_name }}.jar --spring.config.location={{ backend_root }}/{{ backend_name }}/config/application.yml
SuccessExitStatus=143

[Install]
WantedBy=multi-user.target